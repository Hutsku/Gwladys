<!DOCTYPE html>
<% function nl2br (str, is_xhtml) {
    if (typeof str === 'undefined' || str === null) {
        return '';
    }
    var breakTag = (is_xhtml || typeof is_xhtml === 'undefined') ? '<br/>' : '<br/>';
    return (str + '').replace(/([^>\r\n]?)(\r\n|\n\r|\r|\n)/g, '$1' + breakTag + '$2');
} %>
<html>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <meta name="description" content="">
        <!-- favicon -->
        <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
        <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
        <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
        <link rel="manifest" href="/favicon/site.webmanifest">
        
        <!-- Import CSS Bootstrap -->
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
        <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous"> -->

        <!-- Import custom CSS -->
        <link rel="stylesheet" type="text/css" href="/css/style-blog.css">
        <link rel="stylesheet" type="text/css" href="/css/style-navbar.css">

        <title>kita pena</title>
    </head>
 
    <body>
        <header>
            <%- include('navbar') %>
        </header>
        <main class="">
            <div class="article-list container">
            <% if (articles.length) {
            for (article of articles) { %>
                    <div class="article row d-flex flex-column justify-content-between pl-4 mb-4">
                        <div class="article-infos mt-2 mt-md-0">
                            <span class="article-title"><%= article.title %></span>
                            <span class="article-date pb-md-3">Article publié le : <%= article.date.toLocaleDateString() %></span>
                            <span class="article-desc d-block"><%- nl2br(article.description1) %></span>
                            <div class="article-gallery d-flex my-4">
                                <% for (image of article.image) { %>
                                <div class="article-img col col-md-3 mr-3">
                                    <img class="w-100" src="/img/<%= image %>">
                                </div>
                                <% } %>
                            </div>
                            <span class="article-desc d-block"><%- nl2br(article.description2) %></span>
                        </div>
                    </div>
            <% } }%>
            </div>               
        </main>

        <footer>
            <%- include('footer') %>
        </footer>
    </body>
</html>